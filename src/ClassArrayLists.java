import java.util.ArrayList;

public class ClassArrayLists {
    public static void main(String[] args) {
        try {
            int[] numbers = {1, 2, 3}; // 버켓 사이즈를 미리 설정해야함

            // 버켓 사이즈를 미리 설정하지 않아도 사용이 가능함
            arrayList.add(1); //true
            arrayList.add(2); //true
            arrayList.add(3); //true
            arrayList.size(); //3

            ArrayList arrayList = new ArrayList();

            arrayList.set(1, 5); // Integer@35 "2"
            arrayList.remove(0); // Integer@34 "1"
            arrayList.clear(); //(void)

            System.out.println();
        } catch (Exception e) {
            // TODO: handle exception
        }
    }
}
